# Kirbyuse

## Installation 

### Install kirbyuse

1. Open `site/plugins/kirbyuse`
2. Run `npm i`
3. Run `npm run build`

### Install kirbyuse-playground

1. Open `site/plugins/kirbyuse-playground`
2. Run `npm i`
3. Run `npm run dev`